# Pari-Mutuel Phase 2 Invariant Matrix

Date: 2026-02-20  
Scope: prototype-gated planning artifact (no behavior changes)

## Invariant Table
| Invariant | Description | Enforcing Code Path | Test Trace | PASS Criterion | FAIL Criterion |
|---|---|---|---|---|---|
| `INV-01` Buy/Freeze Mutual Exclusion | Buys are only valid while round is `OPEN`; freeze path is valid only in `CLOSED_PENDING_FREEZE`/`CLOSED_FROZEN`. | Buy gating in `programs/openjack/src/instructions/purchase.rs`; freeze state gating in `programs/openjack/src/instructions/round.rs` (`apply_prototype_freeze`). | Buy status-gate tests in purchase module; freeze state-gate behavior exercised by `apply_prototype_freeze_commits_then_is_idempotent_on_retry` and `apply_prototype_freeze_rejects_commitment_mismatch` in `programs/openjack/src/instructions/round.rs`. | No reachable round status allows both successful buy and successful freeze for the same state. | Any state permits a buy and freeze in parallel without transition. |
| `INV-02` Freeze Replay Determinism | Repeated freeze calls for same round state produce identical commitment state (or deterministic mismatch reject). | `programs/openjack/src/instructions/round.rs` (`apply_prototype_freeze`, `freeze_ticket_set`). | `apply_prototype_freeze_commits_then_is_idempotent_on_retry`; `apply_prototype_freeze_rejects_commitment_mismatch`. | Replay calls are no-op/equality-check (or deterministic mismatch error), never divergent commitment output. | Replay changes canonical commitment for same round inputs. |
| `INV-03` Caller-Independence | Caller identity/timing does not affect derived canonical root. | `programs/openjack/src/instructions/round.rs` (`derive_prototype_ticket_set_root`, `apply_prototype_freeze`). | `freeze_commitment_is_derived_only_from_round_state_not_caller_timing`. | Commitment depends only on round state fields; not signer/timestamp variances. | Commitment changes due to caller or call-time metadata. |
| `INV-04` Multi-Caller Determinism | Competing callers (different order) converge to same canonical commitment end state. | `programs/openjack/src/instructions/round.rs` (`apply_prototype_freeze`). | `multi_caller_race_order_keeps_same_canonical_commitment_state`. | Canonical fields match regardless of caller order. | Caller ordering can produce divergent canonical fields. |
| `INV-05` No Deadline-Based Orphan Vector | No timeout should invalidate all completion paths from freeze/count progression. | State-machine policy in `docs/PARI_MUTUEL_CANONICAL_COUNT_SPEC_DRAFT.md` and `docs/PARI_MUTUEL_PHASE2_CHECKLIST.md`. | Spec/checklist policy lock (docs-only gate in Phase 2). | No protocol deadline disables all forward-progress paths. | Any timeout can permanently strand round from completion. |
| `INV-06` First Valid Finalize Wins | `COUNT_FINALIZED` is represented by `count_finalized=true` and `count_progress_index=ticket_count_frozen`; either fast-path finalize or batch terminalization may set it first. | `programs/openjack/src/instructions/round.rs` (`apply_finalize_counts_fast_path`, `apply_count_batch`). | `finalize_counts_fast_path_marks_terminal_count_state`; `finalize_counts_fast_path_short_circuits_partial_batch_progress`; `count_batch_multi_batch_progress_reaches_finalized_end_state`. | First valid finalization path deterministically lands in identical terminal count state. | Different callers/paths can produce divergent terminal count state. |
| `INV-07` Post-Finalization Mutual Exclusion | After `COUNT_FINALIZED`, additional finalization attempts are replay-safe no-op or deterministic reject; no path can mutate terminal count state. | `programs/openjack/src/instructions/round.rs` (`apply_finalize_counts_fast_path`, `apply_count_batch`). | `finalize_counts_fast_path_replay_is_noop_and_immutable`; `count_batch_after_fast_path_finalized_rejects_new_range`; `count_batch_replay_last_batch_after_finalized_is_noop`. | Terminal state remains immutable and replay-safe after finalization. | Any post-finalization call mutates progress/finalized flags or causes non-deterministic outcomes. |
| `INV-08` Fast-Path Binding Integrity | Fast-path finalize must bind exactly to round truth (`round_id`, `ticket_set_root`, `ticket_count_frozen`, draw, tier counts), mock verifier interface bindings (`mock_public_inputs`, `mock_proof`), and verifier-instruction bindings (ed25519 pubkey+message) with specific mismatch errors. | `programs/openjack/src/instructions/round.rs` (`FinalizeCountsFastPathArgs`, `validate_fast_path_bindings`, `validate_fast_path_mock_interface`, `validate_fast_path_verifier_instruction`). | `finalize_counts_fast_path_rejects_round_id_mismatch`; `finalize_counts_fast_path_rejects_ticket_set_root_mismatch`; `finalize_counts_fast_path_rejects_ticket_count_mismatch`; `finalize_counts_fast_path_rejects_winning_main_mismatch`; `finalize_counts_fast_path_rejects_winning_bonus_mismatch`; `finalize_counts_fast_path_rejects_tier_winner_count_mismatch_on_replay`; `finalize_counts_fast_path_rejects_mock_public_inputs_mismatch`; `finalize_counts_fast_path_rejects_mock_proof_mismatch`; `finalize_counts_fast_path_rejects_mock_verifier_false`; mixed-mode short-circuit trace: `finalize_counts_fast_path_short_circuits_partial_batch_progress`; harness trace (includes verifier pubkey/message mismatch): `reports/protocol-gate/fast-path-mock-harness-1771702886962.json`. | Any mismatch in bound fields fails deterministically with specific error code; positive path passes only when all bindings and verifier instruction are valid. | Fast-path finalize accepts mismatched binding fields or ambiguous verifier failures. |
| `INV-09` Bounty Conservation + Deterministic Progress Payout | Under prototype bounty accounting, rewards are paid only on strict accepted progress and are conserved: `bounty_distributed_so_far + bounty_pool_balance == bounty_pool_initial`. Replay/no-op and post-finalization paths pay zero. | `programs/openjack/src/instructions/round.rs` (`apply_prototype_freeze`, `apply_count_batch`, `compute_batch_bounty_reward`, `apply_batch_bounty_accounting`); `programs/openjack/src/instructions/purchase.rs` (bounty accrual); `programs/openjack/src/math.rs` (3% bounty split under prototype). | `count_batch_bounty_conservation_is_monotonic_per_transition`; `count_batch_bounty_rounding_on_last_batch_never_overpays`; `count_batch_noop_replay_pays_zero_bounty`; `partial_batch_then_fast_finalize_blocks_additional_bounty`; `micro_batch_reward_scales_with_delta_progress`. | Bounty distribution is monotonic, never exceeds distributable cap, never overpays on final batch rounding, and remains immutable after finalization. | Any replay/new-range/post-finalization path can pay additional bounty, or conservation identity is violated at any transition. |
| `INV-10` Global Round Solvency Floor | Every debit from round lamports must satisfy: `post_balance >= rent_exempt_min + undistributed_bounty + required_settlement_reserve`, where `required_settlement_reserve = winners_pool_balance + unclaimed_pool_balance`. | `programs/openjack/src/solvency.rs` (`assert_round_solvency_floor`, `required_settlement_reserve`); `programs/openjack/src/instructions/claim.rs`; `programs/openjack/src/instructions/round.rs`. | `solvency::tests::settlement_reserve_is_sum_of_claim_obligations`; `solvency::tests::arithmetic_sequence_model_preserves_floor_terms`; `solvency::tests::edge_of_rent_floor_arithmetic_detects_violation`; plus protocol sequencing tests `partial_batch_then_fast_finalize_blocks_additional_bounty` and replay/no-op bounty tests. | No debit path can reduce round lamports below rent floor plus outstanding economic obligations. | Any debit path can bypass solvency floor and starve claimability/bounty carry or violate rent floor obligations. |

## Notes
- `INV-05` is a policy/spec invariant in Phase 2 checklist scope; executable enforcement is deferred to implementation PRs.
- This matrix is normative for Phase 2 planning and must remain prototype-gated.
